spring:
  application:
    name: boot-ai
  ai:
    chat:
      client:
        enabled: false
    openai:
      api-key:  ${ai.openai.api-key}
    gemini: ${ai.openai.api-key}

server:
  port: 8090

logging:
  level:
    org:
      springframework:
        ai: DEBUG
        opemai: debug
        web:
          client: trace