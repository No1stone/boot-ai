spring:
  application:
    name: boot-ai
  ai:
    chat:
      client:
        enabled: false
    openai:
      api-key:  ${key.ai.chatgpt}
      api-url: https://api.openai.com/v1
server:
  port: 8090

logging:
  level:
    org:
      springframework:
        ai: DEBUG
        opemai: debug
        web:
          client: trace